{% extends "__layout.twig" %}

{% block content %}
{% import "_btn_layout.twig" as forms %}

  <div class="pt-2">
    <p>Вы смотрите страницу - "{{title}}"</p>
  </div>

  <div>
    <ul class="nav nav-pills">
      <li class="nav-item">
        {{ forms.input('Картинка', img_active, "button", img_content) }}
        <!-- <a class="nav-link {{ img_content == url ? 'active' }}" href="{{ img_content }}">Картинка</a> -->
      </li>
      <li class="nav-item">
        {{ forms.input('Описание', info_active, "button", info_content) }}
        <!-- <a class="nav-link {{ info_content == url ? 'active' }}" href=" {{ info_content }} ">Описание</a> -->
      </li>
    </ul>
  </div>
  
  {% block objContent %}

  {% endblock %}
  
{% endblock %}